transforms:
  - !Rename
    from: gpio0::Gpio0
    to: gpio::Gpio

  - !Rename
    from: gpio0::(.*)
    to: gpio::$1

  - !MergeFieldsets
    from: gpio::regs::RegGroup([0123])
    to: gpio::regs::RegGroup
